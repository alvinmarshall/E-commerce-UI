<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    tools:context=".ui.modal.ModalSortFragment">

    <TextView
        android:id="@+id/tv_item_1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:text="@string/label_sort_by"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        app:layout_constraintTop_toTopOf="parent" />

    <ListView
        android:id="@+id/list_view"
        app:layout_constraintTop_toBottomOf="@id/tv_item_1"
        android:layout_width="match_parent"
        android:choiceMode="singleChoice"
        tools:listitem="@tools:sample/first_names"
        android:listSelector="@color/colorSecondary"
        android:layout_height="0dp" />


</androidx.constraintlayout.widget.ConstraintLayout>